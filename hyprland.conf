

###############################
### HYPRLAND CONFIG  ###
###############################

# Docs: https://wiki.hyprland.org/Configuring/
# Tip: you can split config into multiple files and load them with `source = ...`.


################
### INCLUDES ###
################

# Generated by pywal (updated by ~/.config/hypr/scripts/wallpaper.sh)
source = ~/.config/hypr/colors.conf

# Optional split config files (uncomment if you start using them)
# source = ~/.config/hypr/monitors.conf
# source = ~/.config/hypr/workspaces.conf


#################
### VARIABLES ###
#################

# Main modifier key used throughout binds
$mainMod = SUPER

# Apps
$terminal = kitty
$fileManager = dolphin
$menu = rofi -show drun
$browser = zen-browser


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,1920x1080@144,auto,1
# monitor=HDMI-A-1,preffered,auto,1


#################
### AUTOSTART ###
#################

# Keyring / polkit
exec-once = ~/.config/hypr/scripts/start_kerying.sh
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = waypaper --restore &

# Wallpaper: restore what was last set by ~/.config/hypr/scripts/wallpaper.sh
exec-once = ~/.config/hypr/scripts/restore-wallpaper.sh &

# Tray / networking
exec-once = nm-applet &

# Shell / bars
#exec-once = QT_QPA_PLATFORM=wayland quickshell -p /home/nivedck/.config/quickshell/nived/bar-shell.qml &
exec-once = qs -c noctalia-shell
# exec-once = waybar

# Idle / notifications
exec-once = hypridle -c ~/.config/hypr/hypridle.conf
# exec-once = swaync
# exec-once = quickshell --config silly-notifs

# Cursor theme (runtime)
exec-once = hyprctl setcursor WhiteSur-cursors 24

# Clipboard history (cliphist)
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Plugins
exec-once = hyprpm reload -n # Loads plugins on startup


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_THEME,macOS-White
env = XCURSOR_SIZE,24
env = HYPRCURSOR_THEME,macOS-White
env = HYPRCURSOR_SIZE,24
env = XDG_MENU_PREFIX,arch-
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Permission changes require a Hyprland restart and are not applied on-the-fly.

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# General layout / gaps / borders
general {
    gaps_in = 6
    gaps_out =6

    border_size = 2

    # Border colors come from ~/.config/hypr/colors.conf (pywal)
    # col.active_border = ...
    # col.inactive_border = ...

    # Enable resizing windows by clicking and dragging on borders/gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before enabling
    allow_tearing = false

    layout = dwindle
}

# Window decorations: rounding, opacity, blur
decoration {
    rounding =12
    rounding_power = 2

    active_opacity = 0.85
    inactive_opacity = 0.7

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 2
        passes =5
        vibrancy = 0
    }
}

# Animations
animations {
    enabled = yes

    # Some default animations, see https://hypr.land for more
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# Alternative animation presets (kept for reference)
# animations {
#   enabled =  true
#   bezier =  overshot, 0.05, 0.9, 0.1, 1.05
#   bezier =  smoothOut, 0.36, 0, 0.66, -0.56
#   bezier =  smoothIn, 0.25, 1, 0.5, 1
#   bezier =  linear, 0.0, 0.0,1.0,1.0
#
#   animation =  windows, 1, 3, overshot, slide
#   animation =  windowsOut, 1, 4, smoothOut, slide
#   animation =  windowsMove, 1, 4, default
#   animation =  border, 1, 10, default
#   animation =  fade, 1, 10, smoothIn
#   animation =  fadeDim, 1, 10, smoothIn
#   animation =  workspaces, 1, 4, default , #slidevert
#   animation =  borderangle, 1, 30,linear,loop
# }

# Workspace “smart gaps” examples (kept for reference)
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# Layout-specific settings
dwindle {
    # pseudotile = true # toggled via bind: $mainMod + P
    preserve_split = true # keep splits stable
}

master {
    new_status = master
}

# Misc settings
misc {
    force_default_wallpaper = -1 # Set to 0/1 to disable the mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background.
    vrr = 2
}


#############
### INPUT ###
#############

# Keyboard / mouse / touchpad
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1
    sensitivity = 0 # -1.0 - 1.0

    touchpad {
        natural_scroll = true
    }
}

gestures {
    gesture=3,horizontal , workspace
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# Launchers / apps
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, E, exec, $fileManager
#bind = $mainMod, R, exec, $menu
bind = $mainMod, B, exec, $browser
bind = $mainMod, R , exec, qs -c noctalia-shell ipc call launcher toggle

# Session / window actions
bind = $mainMod, U, killactive,
bind = $mainMod, L, exit,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, F, fullscreen , 0 # fullscreen
bind = $mainMod SHIFT, F , fullscreenstate, 0 2
bind = $mainMod, SPACE, togglefloating
bind = $mainMod, Q, exec, /home/nivedck/.config/hypr/scripts/battery.sh
bind = $mainMod, I, exec, /home/nivedck/.config/hypr/scripts/perfomance.sh

# UI helpers
bind = $mainMod, W, exec, ~/.config/hypr/scripts/reload-quickshell.sh
#bind = $mainMod, N, exec, quickshell ipc --any-display -p /home/nivedck/.config/quickshell/nived/bar-shell.qml call sidebarRight toggle
bind = $mainMod, N, exec, qs -c noctalia-shell ipc call controlCenter toggle
bind = $mainMod , S , exec , qs -c noctalia-shell ipc call settings toggle
# Toggle the specific Notification History window/viewer
bind = $mainMod+SHIFT, N, exec, qs -c noctalia-shell ipc call notifications toggleHistory
bind = $mainMod+SHIFT,L, exec, qs -c noctalia-shell ipc call darkMode toggle
bind = $mainMod+SHIFT,S, exec, qs -c noctalia-shell ipc call systemMonitor toggle
bind = $mainMod+SHIFT,M,exec, qs -c noctalia-shell ipc call media toggle


#wallpaper
#bind = $mainMod, M, exec, /home/nivedck/.config/rofi/wallpapers/wallpaper.sh # wallpaper picker
bind = $mainMod,M,exec,qs -c noctalia-shell ipc call wallpaper toggle

# Clipboard
bind = SUPER, V, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy
bind = SUPER SHIFT, V, exec, cliphist wipe
# bind = SUPER, V, exec, ~/.local/bin/clipboard-history

# Screenshots
bind = ,Print, exec, mkdir -p ~/Pictures && grim - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy --type image/png && notify-send "📸 Screenshot" "Full screen captured"
bind = $mainMod,Print, exec, mkdir -p ~/Pictures && grim -g "$(slurp -w 0)" - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S)-region.png | wl-copy --type image/png && notify-send "📸 Screenshot" "Region captured"
bind = $mainMod, S, exec, mkdir -p ~/Pictures && grim - | tee ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy --type image/png

# Focus movement (arrow keys)
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Workspaces: switch
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Workspaces: move active window
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Workspaces: scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Mouse move/resize
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Resize focused window
bind = SUPER SHIFT, left, resizeactive, -30 0
bind = SUPER SHIFT, right, resizeactive, 30 0
bind = SUPER SHIFT, up, resizeactive, 0 -30
bind = SUPER SHIFT, down, resizeactive, 0 30

# Multimedia keys (audio + brightness)
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Media control (requires playerctl)
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


##############################
### WINDOWS AND WORKSPACES ###
##############################

# Rules are optional; leaving examples here for later use.
# See https://wiki.hyprland.org/Configuring/Window-Rules/
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps
# windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
# windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0


###############
### PLUGINS ###
###############

# Example plugin config (hyprexpo)
# plugin {
#     hyprexpo {
#         columns =3
#         gap_size = 20
#         bg_col = rgb(111111)
#         workspace_method = first center # [center/first] [workspace]
#         gesture_distance = 300
#     }
# }
